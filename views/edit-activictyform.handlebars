<div class="row">
<div class="col s12">
<div class="glassy-bg">
 <form method="post" action="/activities/activicty/{{activicty._id}}">  
   <div class="form-group">
    <label>Name</label>
    <input type="text" class="form-control" value = "{{activicty.activicty_name}}" name="activicty_name">
  </div>

  <div class="form-group">
    <label>department Type</label>
    <select name ="activictysub" id= "activictysub">
         <option value="">Choose your Sub activicty</option>         
         <option value="0"{{#ifEquals activicty.parent_id parent}}  selected ="selected" {{/ifEquals}} >Main</option>    
          {{#each allacticties}} 
         <option value="{{ _id }}" {{#ifEquals ../activicty.parent_id _id}}  selected ="selected" {{/ifEquals}}>{{ activicty_name }}</option>    {{/each}}
   </select>
  </div>
  <button type="submit" class="btn btn-default">Update</button>
</form>
</div>
</div>
</div>

