<div class="row">
<div class="col s12">
<div class="glassy-bg">

 <form method="post" action="/departments/projectsassigned/{{projectsassigned._id}}">  
 <div class="form-group">
    <label>Choose your Employees</label>
    <select name ="employees_schedule" id= "employees_schedule">
         <option value=""disabled selected>Choose your Employees</option>
         {{#each users}} 
         {{#ifEquals ../projectsassigned.employee_id _id}}  selected ="selected" {{/ifEquals}}
         <option value="{{_id}}" {{#ifEquals ../ projectsassigned.employee_id _id}}  selected ="selected" {{/ifEquals}}>{{name}}</option>
{{/each}}
    </select>
  </div>

  <div class="form-group">
    <label>Date start</label>   
      <input type="date" class="datepicker" name ="employee_shift_date_start" value="{{projectsassigned.shift_date_start}}">
  </div>  
  <div class ="row fieldgroup">
      <div class="col s2">
          <select name ="activity_category" id= "activity_category" >
           <option value=""disabled selected>Select Main Category</option>    
           {{#each activicty}}
           <option value="{{_id}}">{{ activicty_name}}</option>{{/each}}
          </select>
       </div>
      <div class="col s2">
           <select name ="activity_subcategory" id= "activity_subcategory" >
           <option value=""disabled selected>Select Sub Category</option>    
          </select>
       </div>
      <div class="col s2">
          <select name ="activity_clients" id= "activity_clients" >
           <option value=""disabled selected>Select Clients</option>
            {{#each clients}}
           <option value="{{_id}}">{{ clients_name}}</option>{{/each}}    
          </select>
      </div>
</div>
  
   <button type="submit" class="btn btn-default">Update</button>
</form>
</div>
</div>
</div>

